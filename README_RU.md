# forex-prediction-bot

Компактный Telegram-бот и набор утилит для краткосрочных прогнозов валютных курсов. Проект прост и учебный — ансамбль строится из небольших моделей: марковских цепей для направления и лаговой линейной регрессии для величины.

Репозиторий включает парсер данных, утилиты для обучения моделей, инструменты визуализации и Telegram-интерфейс для интерактивного тестирования.

📘 Англоязычная версия: `README.md`

---

## Краткий обзор

- Цель: компактный исследовательский/демонстрационный проект для короткосрочных прогнозов курсов и интерактивного использования через Telegram.
- Подход: направление — марковские цепи по последовательностям знаков; величина изменения — лаговая линейная регрессия. Конечный прогноз получается как комбинация (ансамбль) этих моделей.

Подходит для: быстрого прототипирования, обучения и экспериментов.

---

## Ключевые модули

Код разделён на небольшие, понятные модули в папке `src/`:

- `src/main.py` — оркестратор: при старте (если включено) обучает модели и запускает Telegram runtime через `telegram_bot.telegram_main(config)`.
- `src/telegram_bot.py` — реализация Telegram-обработчиков, UI и управление состоянием чатов.
- `src/parser.py` — HTTP-сессия и функции получения исторических курсов.
- `src/model_engine.py` — логика моделей: марковские цепи и регрессии + save/load.
- `src/plotter.py` — функции для генерации PNG-графиков прогнозов.

---

## Быстрый старт

Требуется: Python 3.8+ и pip.

1) Создайте и активируйте виртуальное окружение

Windows (PowerShell):

```powershell
python -m venv venv
venv\Scripts\Activate.ps1
```

2) Установите зависимости

```powershell
pip install -r requirements.txt
```

3) Установите переменные окружения в `src/.env` (пример):

```env
TELEGRAM_TOKEN=ВАШ_ТОКЕН_ЗДЕСЬ
# GEMINI_API_KEY=... (опционально для ассистента)
```

4) Запустите бота

```powershell
python src/main.py
```

При первом запуске, если `REBUILD` в `MODELS_SETTINGS` включён, модели будут переобучены и сохранены в `models/`.

---

## Формат данных

Каждый CSV в `datasets/` должен иметь как минимум поля `Date` и `Price` (старые → новые). Репозиторий умеет генерировать знаки (`+`/`-`) и разницы, если их нет.

Пример минимального CSV:

```csv
Date,Price
2005-04-04,35.938
2005-04-05,35.790
2005-04-06,35.845
```

---

## Для разработчиков

- Добавление данных: положите CSV в `datasets/` и перезапустите (или вручную переобучите) модели.
- Изменение UI/логики: редактируйте `src/telegram_bot.py` и убедитесь, что конфигурация прокидывается из `src/main.py`.

Я могу добавить: короткий пример добавления датасета + запуска обучения, небольшую коллекцию unit-тестов или CI-контроль — что предпочтительнее?

---

## Лицензия

MIT — см. `LICENSE`.

---

Компактный Telegram-бот и утилиты для экспериментов с краткосрочными прогнозами валютных курсов. Подход — комбинация небольших марковских моделей для направления и лаговой линейной регрессии для величины изменения.

Этот репозиторий содержит парсер данных, базовый пайплайн обучения, инструменты для визуализации и контроллер Telegram-бота для быстрой интерактивной проверки результатов.

📘 Англоязычная версия: `README.md`

---

## Краткий обзор

- Цель: исследовательский/демонстрационный проект для короткосрочных прогнозов курсов и интерактивного использования через Telegram.
- Подход: направление — небольшие марковские цепи по последовательностям знаков; величина изменения — простая лаговая линейная регрессия; финальный прогноз — ансамбль.

Подходит для: быстрого прототипирования, обучения и демонстрации идеи.

---

## Возможности

- Компактный и читабельный код — удобно читать и расширять
- Загрузчик и парсер CSV (исторические данные в `datasets/`)
- Обучающие скрипты, сохраняющие модели в `models/`
- Визуализация прогнозов (PNG в `temp/`)
- Телеграм-бот (`src/main.py`) для интерактивных проверок результатов

---

## Быстрый старт

Требуется: Python 3.8+ и pip.

1) Создайте и активируйте venv

Windows (PowerShell):

```powershell
python -m venv venv
venv\Scripts\Activate.ps1
```

2) Установите зависимости

```powershell
pip install -r requirements.txt
```

3) Укажите Telegram токен в `src/.env`:

```env
TELEGRAM_TOKEN=ВАШ_ТОКЕН_ЗДЕСЬ
```

4) Запустите бота

```powershell
python src/main.py
```

При первом запуске (если `REBUILD` включён) модели будут обучены и сохранены в `models/`.

---

## Структура

- `src/` — основной код бота и вспомогательные утилиты
- `datasets/` — CSV с историческими ценами
- `models/` — сохранённые модели
- `temp/` — изображения/графики для бота

---

## Формат данных

Каждый CSV должен иметь как минимум колонки `Date` и `Price`, строки — от старых к новым.

Пример:

```csv
Date,Price
2005-04-04,35.938
2005-04-05,35.790
2005-04-06,35.845
```

---

## Модели и прогнозирование (кратко)

- Марковские модели обучаются на последовательностях знаков `+`/`-` и предсказывают направление.
- Регрессии прогнозируют числовое изменение на основе предыдущих лагов.
- Ансамбль объединяет результаты так: большинство марковских голосов → направление; усреднение регрессий → величина; при расходе знаков направление применяется к абсолютной величине.

---

## Конфигурация

Настройки по умолчанию находятся в `src/main.py` в словаре `MODELS_SETTINGS`.

- `REBUILD` — переподготовка при старте
- `markov.min_n / max_n` и `markov.k` — порядок марковских моделей и сглаживание
- `reg.min_n / max_n` — диапазон лагов для регрессий

---

## Для разработчиков

- Проект рассчитан на быстрое обучение и эксперименты — применяйте регуляризацию, расширенные признаки и тесты для улучшения качества.

---

## Лицензия

MIT — см. файл `LICENSE`.

---

Готов продолжить: могу добавить короткий пример запроса к боту, инструкции по добавлению новых данных или отдельный dev-guide — что предпочитаете дальше?